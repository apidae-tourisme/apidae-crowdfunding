<h2>Configuration</h2>
<div id="edit_msg"></div>
<div class="sibu_form sibu_edit_form">
  <%= form_tag(update_section_site_page_path(@site.id, @page.id), method: :patch, remote: true) do |f| %>
    <div class="sibu_field">
      <%= label_tag 'section[id]', 'Identifiant' %>
      <%= text_field_tag 'section[id]', @section["id"], placeholder: 'Doit être unique sur la page' %>
    </div>
    <%= hidden_field_tag :section_id, @section_id %>
    <%= hidden_field_tag :refresh, true %>
    <div class="sibu_actions">
      <%= submit_tag 'Valider' %>
      <%= link_to 'Annuler', '#', onclick: 'cancelEdit(); return false;' %>
    </div>
  <% end %>
</div>
